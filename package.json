{
  "name": "perl-language-server",
  "displayName": "Perl Language Server",
  "description": "a simple javascript based language server for perl",
  "version": "0.0.1",
  "scripts": {
    "compile": "tsc -b",
    "clean": "rm -rf server/out && rm -rf client/out",
    "watch": "tsc -b -w",
    "postinstall": "cd client && npm i && cd ../server && npm i && cd .."
  },
  "engines": {
    "vscode": "^1.46.0"
  },
  "main": "./client/out/extension",
  "activationEvents": [
    "onLanguage:perl"
  ],
  "author": "ganezdragon",
  "publisher": "ganezdragon",
  "repository": {
    "type": "git",
    "url": "https://github.com/ganezdragon/perl-language-server"
  },
  "categories": [
    "Programming Languages"
  ],
  "contributes": {
    "configuration": {
      "title": "Perl",
      "properties": {
        "perl.maxNumberOfProblems": {
          "scope": "resource",
          "type": "number",
          "default": 100,
          "description": "Controls the maximum number of problems produced by the server."
        }
      }
    },
    "languages": [
      {
        "id": "perl",
        "aliases": [
          "perl",
          "perl"
        ],
        "extensions": [
          ".pl",
          ".pm"
        ],
        "configuration": "./language-configuration.json"
      }
    ]
  }
}
